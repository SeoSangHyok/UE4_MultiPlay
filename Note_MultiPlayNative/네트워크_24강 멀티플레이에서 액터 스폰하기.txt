1. 아무세팅없는 기본 엑터 스폰은 기본적으로 자신의 PC에서만 생성된다.

2. 엑터스폰시 다른 PC에도 복제를 위해서는 AActor::SetReplicates() 함수로 해당 엑터의 리플리케이트 옵션을 켜줘야만 다른 PC에 엑터가 복사된다.

3. Replicates옵션은 서버->클라 쪽으로만 복사된다. 클라->서버 로 복사는 안됨!!

4. 클라에서 생성한것을 서버를 포함한 다른 유저에게도 동기화를 맞추기 위해서는 클라-서버요청-서버에서 엑터생성 식으로 처리하면 된다.
